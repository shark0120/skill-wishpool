# Claude → 老師:PR #1 有第二個問題(比動效預算嚴重),以及第二批圖需求

六個代理對線上站做了一輪唯讀稽核(只 GET,沒有許願、沒有附議)。程式與部署面是乾淨的,
但 PR #1 被抓到**第二個問題**,而且那個問題量尺抓不到 —— 先講這個。

---

## PR #1:動畫在現站的 CSS 下等於看不見

`class="i solid"` 沒有拿掉。現站的規則是:

```css
.i{stroke:currentColor;fill:none;stroke-width:1.7}
.i.solid{fill:currentColor;stroke:none}      /* ← 這一條 */
```

`.i.solid` 是 class 選擇器(specificity 0,2,0),會**蓋掉**寫在 `<svg>` 標籤上的
`fill="none" stroke="currentColor"`(presentation attribute 的 specificity 是 0)。
而且 fill/stroke 是可繼承屬性,子元素沒有自己的宣告就一起吃到。結果:

1. 想要的**空心水滴變成實心**(跟現站原本那顆一樣)。
2. `.cta-fill` 那塊 `opacity:.28` 的水位矩形,疊在一顆已經 100% currentColor 的實心水滴上
   —— **水位上升完全看不出來**。
3. `<circle class="cta-ripple">` 沒有自己的 fill/stroke,繼承到 `fill:currentColor; stroke:none`
   —— **漣漪從「擴散的細圈」變成「實心圓餅」**。

也就是說:就算把 `infinite` 從 5 降回 4、CI 全綠,合進去也只是把一顆實心水滴換成另一顆實心水滴。

**最小修法**:`class="i solid"` → `class="i"`(讓 `.i` 的描邊規則生效),
`<circle class="cta-ripple">` 補上 `fill="none"`。
但這會讓 CTA 從「實心水滴」變成「描邊水滴」—— **是肉眼看得出來的外觀改變**,
所以這個決定是老師的,不是我們替你按。

## 順帶:漣漪會被 viewBox 切到

`<circle cx="12" cy="20.2" r="2.4">` 放大到 `scale(2.6)` 之後半徑 6.24,下緣到 y=26.44,
而 viewBox 底邊是 y=24 —— 最後一段會被切掉一塊。要嘛接受、要嘛把 `cy` 往上移或把 scale 調小。

## 動效預算(上一封講過的那條)還在

`infinite` 3 → 5,上限 4,`selftest` 紅一項、CI 會紅。兩條路:
併成一條 keyframes(用 CSS 自訂屬性餵不同位移),或把漣漪拿掉只留水位。
**不要**用 `animation: var(--x) 3.4s infinite` 把名字藏進變數讓字串計數變 1 ——
那是把量尺騙綠、動效一條沒少,跟這個 repo 自己 `--verify-gauge` 的立場相反。

---

# 第二批圖需求(稽核順出來的,依急迫排序)

## ⑦ 每則願望自己的分享卡底圖(最有價值的一張)

1. **用途**:`/w/{id}` 的 og:image 底圖模板,之後由 `scripts/make_og.py` 把願望標題排上去
2. **規格**:1200×630、**左側留一塊安全區**(可放 2 行標題 + 1 行說明,約 900×300)、
   右下角留一條站名列、**底圖本身不要烤任何字**
3. **提示詞**:
   > Same warm paper world as the main share card but emptier: a large calm area on the left
   > two thirds intentionally left blank for typography, a single gold hairline running under
   > where the title will sit, and one wave stroke anchoring the lower left. Right third: a
   > drop mid-fall with two faint ripple rings below it, deep wine and gold only. No text, no
   > numbers, no frame in the safe area.
4. **給誰用**:動態合成用底圖。**現在十則願望貼進 LINE 長得一模一樣**(全部共用
   `public/og.png`),這是這輪主打「每則願望有自己的網址」最後缺的一塊

## ⑧ apple-touch-icon

1. **用途**:iOS「加到主畫面」的圖示(現在完全沒有,只有一個 data: URI 的 SVG favicon)
2. **規格**:180×180 PNG,另外要 192 與 512 的 maskable 版本;**不透明背景**
   (iOS 不吃透明,會變黑底)
3. **提示詞**:
   > The three-stroke wave mark centred on a warm paper background, deep wine strokes,
   > generous margin so it survives iOS corner masking. Flat, no gradient, no shadow, no text.
4. **給誰用**:`public/` 底下的靜態檔,現站 head 補 `<link rel="apple-touch-icon">`

## ⑨ 五個分類的小記號

1. **用途**:「做出來的 skill」現在分五類(上線 / 驗證 / 代理 / 維運 / 手感),
   14 個項目全是純文字,分類只有標題與數字
2. **規格**:24×24 一組五個、`currentColor`、stroke 1.7、靜態、要跟現有 10 個同一家人
3. **提示詞**:
   > Five sibling line icons, identical weight and rhythm: (a) a box being lifted onto a
   > platform — shipping; (b) a magnifier over a checkmark that is *not* yet ticked —
   > verification; (c) two small squares connected by a line, one leading — agents;
   > (d) a wrench crossed with a heartbeat line — operations; (e) an open hand with a small
   > spark above the palm — craft. No text, no faces, no logos.
4. **給誰用**:紙底 currentColor;這區之後只會愈長愈多,沒有視覺錨點掃視成本會線性上升

## ⑩ 「一輪怎麼跑」流程圖

1. **用途**:頁面底部規則區旁邊(或取代其中一兩句文字)
2. **規格**:橫式,亮暗兩版或純 currentColor 線稿;**不要烤數字**(3 天、門檻都是可調參數)
3. **提示詞**:
   > A simple horizontal flow in one line weight: a drop falls into a pool → two more drops
   > join it → a clock closes the segment → the winning drop leaves the pool and becomes a
   > small folder/box → the box sits on a shelf. A dotted line loops the non-winning drops
   > back into the pool. No text labels, no arrows with heads that look like play buttons.
4. **給誰用**:規則區。現在五條規則全靠文字互相指涉(「那兩個」「上面那一格」),
   **這輪稽核就抓到一句因為區塊改名而變成假話的**;有圖之後文案可以縮短成不指涉的短句

## ⑪ 空狀態插圖:還沒有第一個成品

1. **用途**:「已實現 0」這件事現在只是一個冷冰冰的 0
2. **規格**:寬 > 高的橫幅比例、線稿或極簡色塊、**不要烤字**
3. **提示詞**:
   > A still pool at rest before anything has landed: flat water line, one drop suspended
   > high above it, the surface not yet broken. Quiet and expectant, not sad, not empty-state
   > cliché (no crying faces, no empty boxes). Paper white, wine line, one gold hairline horizon.
4. **給誰用**:第一輪還沒結算完之前的誠實畫面 —— 有圖撐著,文案就不必為了好看而說謊

---

## 這一輪我們自己修掉的(給老師參考,不用出圖)

線上文案有幾句被稽核抓到不誠實,已經改好上線:「誰做」那句原本指到已改名的區塊、
還把站上 14 支說成池子出線做出來的(其實第一輪還沒結算);頁尾補上 localStorage 與
「站在 Cloudflare 後面」;安裝說明原本叫人「下載那個資料夾」但 GitHub 做不到,已換成
真的跑得動的 clone + cp。

還有兩件事**不在程式碼裡、要在儀表板處理**,已經回報給小萱:線上 `robots.txt` 被
Cloudflare 的 Managed 版整份蓋掉(把 ClaudeBot / GPTBot 等九個爬蟲 Disallow,跟站方
自己寫的「歡迎 AI 抓取」相反),以及 `www.skill-tw.com` 目前會落到同機另一個站。
