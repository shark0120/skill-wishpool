# 安全問題回報

## 怎麼報

用 GitHub 的 [Private vulnerability reporting](../../security/advisories/new)(Security 分頁 →
Report a vulnerability)。**不要開公開 issue**,尤其是可以直接被拿去打別人架的池子的問題。

請附上:怎麼重現、影響是什麼、你用的版本或 commit。

## 有人自己架了池子,受影響的範圍

這個專案是給人自己架的,所以修好之後我只能改這個 repo —— 已經架起來的站要站長自己更新。
因此:

- 影響到**已部署站台**的問題(可繞過的速率限制、可越權的管理端、可注入的欄位)會發 GitHub Security Advisory,並在 README 標注受影響版本。
- 只影響**開發環境**的問題(測試腳本、CI)直接開 PR 就好。

## 已知的限制,不用回報

這些是刻意的設計取捨,已經寫在 [README 的「防濫用與隱私」](README.md#防濫用與隱私實際做了什麼):

- 沒有帳號系統;同一個 NAT 底下的人共用一個來源雜湊,IPv6 則是同一段 /64 共用一個
  (收斂到 /64 是刻意的:不收的話,任何有 IPv6 的人手上就有一整段位址可以當成不同來源)。
- 蜜罐欄位只是嚇阻,不是防護。
- 送出前有工作量證明,但**那不是人機驗證** —— 它讓自動化變貴,不能證明對方是人類。
- 沒有第三方 CAPTCHA(會破壞零外部請求),被鎖定攻擊要在反向代理層擋。
- 管理端只有單一權杖,沒有多人權限或操作稽核。
- `WISHPOOL_TRUST_PROXY=1` 在**沒有**反向代理的情況下開啟,速率限制與一人一票就能被 `X-Forwarded-For` 繞過 —— 這在 README 與程式註解都寫了警告,誤用不算漏洞。

如果你能繞過上面那幾條**以外**的任何防護,那就是漏洞,請回報。
