# Claude → 老師：出圖提示詞（2026-08-19 第一批）

照 `FOR-CLAUDE.md` 的格式寫的，一批 6 則。語彙守「許願／聯署／熱門／最新／最多聯署／
領先／已實現／水波／AI skill」，沒有公益句、沒有假數字。

寫這批的人今天也在改站本身（分享網址 `/w/{id}`、og 分享卡、頁面規則說明），
所以下面每一則都對得上現站真的缺的東西，不是憑空想的。

---

## 先講一件會擋到 PR #1 的事

PR #1 我拉下來**在本機試合過**（合完就刪，沒推）：

- **不衝突**，跟 8/19 那批改動不在同一段，`git merge` 乾淨過。
- **`wave-clip` 一字沒動**，10 個 symbol 的 id 與 viewBox 都對得上，是真的 drop-in。
- **但會踩到專案自己訂的動效預算**：規則是「手寫 CSS 裡無限循環動畫 ≤ 4 個」
  （`scripts/selftest.py` 在量，理由是別讓每張卡都在燒電池）。
  main 現在 3 個，CTA 那顆水滴帶進 `cta-fillup` + `cta-ripple` 兩個 `infinite` → 變 5。

```
FAIL 無限循環動畫不超過 4 個  ← 手寫 CSS 裡有 5 個
203 項檢查，1 項失敗
```

最小的兩種解法，請老師選（這是老師的設計，這邊不擅自改）：

1. **兩個 keyframes 併成一條** —— 注滿到頂那一刻順勢把漣漪帶出來，`animation` 只留一條，
   總數回到 4，剛好在預算內。
2. **改成不無限循環** —— 只在 hover／focus 或首次進場跑一次。這條其實最合那個規則的本意
   （主 CTA 永遠在動最耗電），而且「注滿一次」比一直重播更像一個動作。

選好告訴小萱，這邊照做、跑綠了再送回去。下面第 ⑤ 則就是「單循環版」的提示詞。

---

## ① 分享卡 og:image —— 這是唯一該烤字的一張

1. **用途**：現站 `public/og.png`（貼進 LINE／Threads／X 時對方看到的那張，不是介面元件）
2. **規格**：1200×630 靜態、**允許烤字**（社群卡沒字等於沒用，跟「介面不烤字」不衝突）、
   不要任何數字（票數會變，卡片會被爬蟲快取住）
3. **提示詞**：
   > Warm paper-white canvas (#F7F5F2) with a faint gold aurora bleeding in from the upper
   > left and a deep wine glow at lower right. A hairline gold double frame inset from the
   > edge. Left-aligned: three hand-drawn wave strokes in deep wine (#6E2639), then a large
   > serif Traditional Chinese title with wide letter-spacing, a short gold rule beneath,
   > two lines of quiet gray subtitle. Bottom right: three concentric gold ripple arcs
   > clipped by the frame, as if a drop just landed off-canvas. Editorial, generous
   > whitespace, no gradients on text, no glow, no 3D, no photo texture.
4. **給誰用**：現站 og:image。字要能自己排（標題「Skill 許願池」、副標兩行、頁尾一行網域）。
   **金色只用在描邊與漸層，不拿來寫字** —— 淺底上的金字過不了 WCAG AA，站上所有文字都實測過。

## ② 空池 `i-empty`

1. **用途**：現站 icon（補 sprite）—— 搜尋不到、或池子空的時候那一格
2. **規格**：24×24、`currentColor`、stroke 1.7、靜態、無字
3. **提示詞**：
   > A shallow empty basin drawn with two open wave strokes inside it, one tiny drop
   > suspended above the rim about to fall. Single-weight line icon, rounded caps, no fill,
   > no shading, generous negative space, reads at 24px.
4. **給誰用**：紙底 currentColor，亮暗兩套都靠 currentColor

## ③ 附議 `i-vote`

1. **用途**：現站 icon（附議鈕現在是純文字「附議／已署」，想加一個小記號）
2. **規格**：24×24、`currentColor`、stroke 1.7、靜態
3. **提示詞**：
   > A water drop merging into a rising surface line — the drop's tail still visible, the
   > surface bulging where it lands. Reads as "one more added", not as a checkmark, not as
   > a heart. Single-weight line, rounded caps, no fill.
4. **給誰用**：紙底 currentColor；**要跟 `i-drop` 一眼看得出不同** ——
   那顆是「我要許願」，這顆是「我也要」

## ④ 已實現 `i-granted`

1. **用途**：現站 icon（願望狀態徽章）
2. **規格**：24×24、`currentColor`、stroke 1.7、靜態
3. **提示詞**：
   > A drop that has become a small solid seal — outline drop shape with a short horizontal
   > bar inside, like a stamp mark. Calm and final, not celebratory. Single-weight line,
   > rounded caps.
4. **給誰用**：紙底 currentColor；綠色由現站的 `--good` 給，圖本身不帶顏色

## ⑤ CTA 水滴微動畫（單循環版，對應上面的解法 1）

1. **用途**：現站 CTA 微動畫，取代 PR #1 那一版
2. **規格**：inline SVG、24×24、`currentColor`、**只准一條 `animation`**（預算只剩 1 格）、
   要有 `prefers-reduced-motion` 全停
3. **提示詞**：
   > One 3.4s loop: liquid rises inside the drop outline from bottom to full, holds briefly,
   > then the surface releases a single ripple ring that expands and fades as the liquid
   > drains back. Everything on one keyframe timeline, one animated element if possible.
   > currentColor only, no glow, no color shift.
4. **給誰用**：只給「我要許願」inline，不要塞進 `<use>`

## ⑥ 說明區的四個小記號

1. **用途**：現站 icon —— 頁面底部「這個池子怎麼運作」四條規則的行首（純裝飾、不可點）
2. **規格**：24×24 一組四個、`currentColor`、stroke 1.7、靜態、
   風格必須跟現有 10 個是同一家人
3. **提示詞**：
   > Four sibling line icons, same weight and rhythm: (a) a hand holding a small tool —
   > "who makes it"; (b) a three-segment cycle arrow — "every three days"; (c) two drops
   > side by side with the second slightly larger — "someone else's vote counts";
   > (d) an open notebook with a single line written in it — "if it can't be done, it gets
   > written down". No text, no numbers, no faces.
4. **給誰用**：紙底 currentColor；(c) 要能一眼看出是「**別人**那一票」，不要畫成兩滴一樣大
   —— 現站的門檻規則就是「只算不是許願者本人投的票」

---

## 沒有動的東西

照 `CLAUDE-認領.md` 的紅線：cyber 那包沒有拿去 24px、`wave-clip` 沒碰、hero canvas 與文案沒動、
`showcase/01–06` 沒有拿來當主視覺、沒有寫任何公益句。PR #1 也沒有合 —— 等老師選完上面那兩條。
