# 一起把願望做出來

這個池子有兩種參與方式:**許願**,或者**去實現別人的願望**。

## 我想許願

直接到池子的網頁上寫就好,不用註冊。看不到網頁(或那個站關了)的話,
在這個 repo 開一張 [願望單](../../issues/new?labels=wish&template=skill-wish.yml)。

寫得好的願望長這樣:

- **講你卡在哪,不要只講你要什麼工具。** 「把發票 PDF 整理成報稅表格」比「做一個 PDF 工具」有用一百倍。
- **講現在你怎麼做這件事。** 別人才知道要省掉哪一段。
- **講成功長什麼樣子。** 「跑完給我一份 CSV,金額對不上的自己標紅」——這樣才驗收得了。

## 我想實現一個願望

1. 挑一個(建議挑得標過的,首頁「歷屆結果」裡有)。
2. 在那個願望對應的 issue 留一句「我來做」,免得兩個人做一樣的。
3. 做完把成品放哪都行 —— 自己的 repo、gist、部落格。
4. 回來說一聲,池主會把願望標成 `granted` 並掛上你的連結。

沒有規定要用什麼語言、什麼框架。**唯一的要求是別人真的裝得起來、跑得動。**

## 我想改這個池子本身

```bash
python3 scripts/selftest.py                 # 全部要綠
python3 scripts/selftest.py --verify-gauge  # 五個防護拆掉都要讓測試變紅
python3 scripts/check_contrast.py           # 改過顏色就一定要跑
```

三支都要過才送 PR。CI 會再跑一次,而且會跨 Python 3.9–3.13。

幾條這個專案的硬規矩:

- **零依賴。** 後端只用 Python 標準函式庫,前端不外連任何東西(沒有 CDN、沒有字型、沒有分析)。
  這不是潔癖 —— 是為了讓任何人 clone 下來就能跑、離線也能跑。
- **前端永遠不碰 `innerHTML`。** 使用者內容一律 `textContent`。自我測試會掃原始碼,加回去就變紅。
- **改了防護就要改 `MUTATIONS`。** `scripts/selftest.py` 底部那張表用**內容錨點**定位程式碼,
  錨點對不上會直接失敗而不是安靜跳過。這是故意的。
- **改顏色就要重驗對比度。** `--faint`(輔助文字)最容易掉到 4.5:1 以下,開發時就掉過一次。
- **檔案一律 LF。** 有 `.gitattributes` 管,但產生檔案的腳本要自己寫 `newline="\n"`。
- **不要偷偷放追蹤。** 任何會發出外部請求的東西都不會被合併。

## 回報安全問題

見 [SECURITY.md](SECURITY.md)。**不要開公開 issue。**
