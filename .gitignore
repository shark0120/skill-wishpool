# 資料與祕密:絕對不要進版控。
# **不綁目錄** —— --db / WISHPOOL_DB 可以把資料庫開在任何地方(rate limit 與
# 一人一票的來源雜湊、還有同層自動生成的 ip_salt 都在裡面),只擋 data/ 底下
# 等於在別的路徑跑一次就漏了。
*.db
*.db-wal
*.db-shm
*.db-journal
ip_salt
data/*.db
data/*.db-wal
data/*.db-shm
data/ip_salt
*.env
.env

# 建置產物與依賴:vendor 打包結果會被內嵌進 public/index.html 一起進版控,
# 但打包中間檔與 node_modules 不進來。
node_modules/
build/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 編輯器與系統
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
